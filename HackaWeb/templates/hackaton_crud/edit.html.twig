{% extends 'base.html.twig' %}

{% block title %}Edit Hackaton{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Hackaton</h1>

        <div class="row">
            <div class="col-md-6">
                {{ include('hackaton_crud/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-6">
                <a href="{{ path('app_hackaton_crud_index') }}" class="btn btn-secondary">Back to List</a>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-6">
                {{ include('hackaton_crud/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
