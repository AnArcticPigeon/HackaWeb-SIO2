<form method="post" action="{{ path('app_hackaton_crud_delete', {'id': hackaton.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hackaton.id) }}">
    <button class="btn">Delete</button>
</form>
